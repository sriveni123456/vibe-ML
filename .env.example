# ═══════════════════════════════════════════
#  Vibe ML — Environment Variables
#  Copy this to .env and update values
# ═══════════════════════════════════════════

# Server
HOST=0.0.0.0
PORT=8000
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,https://vibeml.in

# Database (SQLite by default — change to PostgreSQL for production)
DATABASE_URL=sqlite:///storage/vibeml.db
# DATABASE_URL=postgresql://user:pass@localhost:5432/vibeml

# JWT Auth
JWT_SECRET=CHANGE-THIS-to-a-random-64-character-string-in-production
TOKEN_EXPIRE_HOURS=72

# Rate Limiting
RATE_LIMIT_PER_MINUTE=30

# Razorpay (for payments — get keys from https://dashboard.razorpay.com)
# RAZORPAY_KEY_ID=rzp_test_xxxxx
# RAZORPAY_KEY_SECRET=xxxxx

# Email notifications (for feedback alerts)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=app-password
# FOUNDER_EMAIL=founder@vibeml.in
